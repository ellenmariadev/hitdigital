# Em desenvolvimento, deixe vazio para usar o proxy do Vite (evita CORS):
VITE_API_BASE_URL=
# Em produção, aponte para a URL pública do backend:
# VITE_API_BASE_URL=https://api.exemplo.com

# Base do provider externo (link "abrir no provider" nos cards):
# VITE_USER_PROVIDER_URL=https://jsonplaceholder.typicode.com/users

# Limite de IDs por consulta (validação client-side; deve bater com MAX_USER_IDS do backend):
# VITE_MAX_USER_IDS=500
